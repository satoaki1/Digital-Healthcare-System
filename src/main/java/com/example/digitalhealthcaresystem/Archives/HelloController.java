package com.example.digitalhealthcaresystem.Archives;

public class HelloController {
    // This is the Controller to execute the program without issues.
    // Since this controller is set in default, it could not be deleted.
}
